import { SlicePagesPipe } from './slice-pages.pipe';

describe('SlicePagesPipe', () => {
  it('create an instance', () => {
    const pipe = new SlicePagesPipe();
    expect(pipe).toBeTruthy();
  });
});
