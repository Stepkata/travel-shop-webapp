import { HistoryStatePipe } from './history-state.pipe';

describe('HistoryStatePipe', () => {
  it('create an instance', () => {
    const pipe = new HistoryStatePipe();
    expect(pipe).toBeTruthy();
  });
});
